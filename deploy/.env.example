# 访问域名（已在 Cloudflare 解析到本机）
DOMAIN=poker.example.com
# 数据库密码：openssl rand -hex 16
POSTGRES_PASSWORD=change-me
# 认证密钥：openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-to-a-long-random-string
# Google 登录（可先留空）
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
