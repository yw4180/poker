{$DOMAIN} {
    encode gzip
    # API、认证与 WebSocket 走后端
    handle /api/* {
        reverse_proxy server:4000
    }
    handle /socket.io/* {
        reverse_proxy server:4000
    }
    handle {
        reverse_proxy web:3000
    }
}
